# TSB API Upstream Configuration
# This should be included in the http block

# Upstream server for TSB API
upstream tsb_api_backend {
    server 127.0.0.1:8006;
    keepalive 32;
    keepalive_timeout 60s;
    keepalive_requests 100;
}